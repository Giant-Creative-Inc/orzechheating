<?php
/**
 * DEPRECATED / DISABLED — Task task_mrl7wf0sj1mn06o3r7
 *
 * This file lived at the repository root under mu-plugins/, which is NOT a
 * proven WordPress mu-plugins deployment root for this site (the deployable
 * path is wp-content/mu-plugins/). It also duplicated meta output and risked
 * emitting a second <meta name="description"> tag.
 *
 * The single source of truth is now:
 *   wp-content/mu-plugins/orzech-page-meta.php
 *
 * This stub is intentionally inert: it defines no hooks and outputs nothing,
 * so even if a server were misconfigured to load it, it cannot create a
 * duplicate meta tag. Do not add logic here — edit the canonical plugin.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Intentionally no hooks, no output. See wp-content/mu-plugins/orzech-page-meta.php.
