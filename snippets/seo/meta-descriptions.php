<?php
/**
 * REFERENCE ONLY — DO NOT ENABLE — Task task_mrl7wf0sj1mn06o3r7
 *
 * The live meta output for the three commercial pages is handled by the
 * canonical mu-plugin: wp-content/mu-plugins/orzech-page-meta.php.
 *
 * This snippet previously described an alternative theme-include approach.
 * Loading it alongside the mu-plugin would produce duplicate title/meta
 * output, so it is deliberately left as an inert reference and registers no
 * hooks. If you ever switch to a theme-include strategy, remove the mu-plugin
 * first so exactly one implementation is active.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Intentionally inert. Canonical implementation: wp-content/mu-plugins/orzech-page-meta.php
